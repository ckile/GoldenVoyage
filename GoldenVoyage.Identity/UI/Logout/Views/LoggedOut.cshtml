@model GoldenVoyage.Identity.UI.Logout.LoggedOutViewModel

<div class="page-header">
    <h1>
        注销
        <small>您已注销成功！</small>
    </h1>

    <div hide-if-null="Model.ReturnInfo">
        Click <a href="@Model.ReturnInfo.Uri">点击这里</a> 返回
        <span>@Model.ClientName</span> 程序.
    </div>

    @foreach (var url in Model.SignOutIFrameUrls)
    {
        <iframe class="signout" src="@url"></iframe>
    }
</div>
